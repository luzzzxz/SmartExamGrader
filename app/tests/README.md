# 运行安全回归测试

最新验证（2026-09-17 按需导出修订）：112 项通过。已替换自动重建测试，覆盖真实浏览/关闭回调不改分、不导出、不上传，以及明确导出使用最新评分。详见《按需导出修复_20260917.md》；下文数量为各阶段历史记录。

在项目包根目录运行：

```powershell
py -3.14 -m unittest discover -s app/tests -v
```

主程序导入使用包内 Python 3.14 依赖。测试写入均在临时目录；网页测试只绑定本机随机端口；大模型 HTTP 返回使用模拟数据。不会上传学生卷、消费模型额度或修改原测试数据库。

若原测试数据库存在，会只读抽取每种已有模板的一份结果，验证临时数据库往返保存；不存在时跳过该项。若安装 Node.js，会额外检查服务端生成的 JavaScript 语法。

2026-09-14：34 项测试通过。覆盖保存失败、损坏 JSON、历史答案快照、测试绑定、分数边界、模型错误响应、网页并发、上传失败和本地回写验证。

完整验证范围及尚未验证项目见 [运行风险检查与优化记录](../项目运行风险检查与优化记录_20260914.md)。

2026-09-16：86 项测试通过。新增跨测试/方案隔离、历史快照、原图版本、旧窗口、透打顺序和失败保留、网页任务版本与重新上传回归；详见 `test_data_isolation.py` 和 `test_runtime_safety.py`。

2026-09-17：94 项测试通过，新增单空答案更正→窗口刷新→关闭保存完整逻辑测试，验证其他空和人工分不变，以及不再自动清空版本冲突成绩。

2026-09-17 后续：114 项测试通过。新增逐空规则迁移、无关设置修改、单空更正后数据库总分及排名联动、真实临时 HTML/Excel/PDF 重建、输出归属登记、替换回退、网页课件命名隔离和失败重试。详见《评分修订与输出联动_20260917.md》。
