# 答题与试卷批改系统 · 软件更新日志 (Changelog)

> **维护说明**：  
> 本文件记录系统的每一次功能更新、缺陷修复、UI交互优化以及数据架构变更。  
> 每次修改代码、新增功能或调整界面逻辑后，均在此保持同步更新，以便随时查阅版本演进历史。

## 2026-09-17：改为用户主动导出

- 浏览、保存、退出和单空更正均不再自动重建或上传 PDF、HTML、Excel；主动导出时读取当前测试最新成绩。
- 去掉浏览刷新中的隐式撤分/补判，避免只查看结果就改变评分。
- 保留总分、排名、统计及预览更新；保存时间等元数据不再造成预览误刷新。
- 112 项测试通过，已替换此前自动重建测试；详见《按需导出修复_20260917.md》。

## 2026-09-17：逐空规则版本与成绩输出联动

- 将测试/结构归属与评分规则修订分开：按空位识别受影响自动分，保留其他空、人工分和历史证据。
- 单空更正联动数据库总分、排名、预览缓存及已登记的透打、整卡、老师讲评 PDF、HTML/Excel 课件。
- 输出登记校验测试归属、文件摘要和原导出参数；替换失败回退，网页失败保留待重试状态。
- 网页课件按测试隔离同名文件，检查配套 Excel 上传结果；透打与课件统一并列排名。
- 114 项回归测试通过；边界及详细说明见《评分修订与输出联动_20260917.md》。

## 2026-09-17：修复单空答案更正被拒绝及自动清空回归

- 同一测试内单空更正先核验原版本，只重判目标空，再更新成绩版本；其他空和默认保护的人工分不变。
- 快速批改窗口刷新时同步版本元数据，修复更正后关闭窗口仍被拒绝保存。
- 取消打开/准备批改时自动返回空成绩的行为，真实版本冲突保留原记录。
- 从已验证备份恢复 `24-6\14.2-14.3` 46 份、`测试班级\14.2-14.3` 3 份记录，仅按更正后的答案重判第 6 空，并同步数据库总分。
- 94 项回归测试通过。详见《单空答案更正修复与成绩恢复_20260917.md》。


## [v2.7.6] - 2026-09-16

### 班级、测试、答案方案与透打隔离加固

- 切断历史测试从其他/共享方案自动补答案、旧成绩跨测试合并和同目录自动猜选测试的路径。
- 新文件夹清除旧方案；横线配置按测试保存；恢复快照核验测试/模板身份及模板图。
- 旧成绩不得改盖新答案签名；重新评分前备份旧记录。保存和透打校验班级、图片目录、答案结构及原图版本。
- 统一自然页序；透打缺图或渲染失败停止整批，避免跳页错位；阻止旧窗口和异步任务写入其他测试。
- 网页任务按测试及答案/结构/原图版本隔离；本地服务端源码新增上传版本和旧网页回写校验。VPS 尚未部署此服务端修改。
- 86 项离线/本机测试通过。现有 89 个测试、1,759 条结果只读核查，未自动重算历史成绩。
- 详细根因、验证及使用变化见《数据隔离排查与修复_20260916.md》，历史项目见《数据隔离历史核查清单_20260916.md》。

---

## [v2.7.5] - 2026-09-16

### 修复切换测试导致主观题裁图与成绩跨试卷污染的严重缺陷

- **问题根因**：`app/core/db_session.py` 中的 `load_session_template_config_snapshot` 误调用了 `merge_subjective_scheme_into_template_config(current_config, snapshot)`。由于该方法以内存旧试卷结构为基准，导致切换打开已保存测试（如《7.1-8.4》）时，前一次打开的试卷（如《13.3-14.1》）的裁图框直接覆盖了当前试卷，进而引发主观题项被缩减（由 13 题变成 6 题），导致大量题项漏计分数且主观题裁图错位。
- **核心代码修复**：
  1. 修复 `load_session_template_config_snapshot`：改为直接权威加载当前测试所属的 `snapshot` 快照，严格禁止与内存遗留的旧试卷裁图做合并。
  2. 完善 `load_session_results`：载入新测试前显式清空 `self.subjective_config = {}`，阻断跨试卷残留污染。
- **数据与报表恢复**：
  1. 完整恢复《7.1-8.4》的 13 项主观题几何结构（第8、9、10、11题填空与12、13题作图题）。
  2. 重新核算并写回数据库 `session_results` 中全部 46 名学生的客观题、主观题与总分，完全对齐已透打的原试卷真实成绩。
  3. 清理学生裁图目录中的冗余异常图片，校正 `_crop_manifest.json`。
  4. 重新生成《【讲评课件】24-6_7.1-8.4.html》与《【讲评数据包】24-6_7.1-8.4.xlsx》。

---

## [v2.7.4] - 2026-09-14

### 运行可靠性、成绩保存与网页评分修复

- 保存前先核对测试 ID、内存与数据库的文件夹绑定，阻止错班成绩/快照写入；保存失败不再静默忽略。
- 历史测试恢复时使用自身答案快照，不因共用方案文件更新而自动替换历史答案。
- 新增 `core/persistence.py`；主程序 JSON 保存和测试快照采用原子替换；损坏或身份不符的成绩文件停止加载，保留原件。
- 网页人工评分增加并发互斥、成绩原子保存、学生/空位/满分校验；浏览器仅在服务器确认成功后跳题，失败提示重试。
- 上传任务先完整校验和解压，再替换原任务并保留有效旧分数；限制上传/解压规模，修复图片路径边界和学生信息 HTML 注入风险。
- 本地取网页分先整批核对学生、空位和分数，异常时停止合并并保留远端任务。
- 模型非 JSON、缺失/非法分数和非有限数值进入失败/待人工路径；0 分题满分不再被替换为 1。
- 按 v2.7.3 保留自由填空同向趋势给分和选填原词要求；修复严格模式误杀同向词，避免把否定或复合趋势按子串自动给满分。
- 移除源码内接口密钥，保留既有私密配置，支持 `ANSWER_CARD_LLM_API_KEY` 环境变量。
- 稳定启动器先加载便携依赖再导入 Pillow；子进程超时/启动失败进入已有失败处理，隐藏 Windows 子进程控制台。
- 备份脚本补入大模型模块、配置、更新日志、检查报告和测试目录。
- 验证：34 项回归测试通过；24 个源文件语法解析通过；PowerShell 备份脚本和网页 JavaScript 语法检查通过。现有数据库只读完整性正常。
- 修复启动恢复测试与打开测试时的严重卡顿问题：移除 `load_session_results` 中误调用的 `repair_loaded_direct_paper_orientations` 重复识别逻辑，严格遵守“读取已保存测试不得重新识别试卷或改写分数”的原则，消除全量试卷 CV 重新处理，启动恢复耗时由 180+ 秒降至 0.5 秒。
- 修复 `repair_loaded_direct_paper_orientations` 中对背面图片错误执行正面角标比例判定的问题，避免背面误判触发重新识别。
- 尚未验收完整 GUI、真实 OCR/模型、VPS 部署与 PDF 打印；原成绩、模板、答案及私密配置未改写。详细范围和剩余风险见 [本次检查记录](项目运行风险检查与优化记录_20260914.md)。

---

## [v2.7.3] - 2026-09-10

### 🔬 重点升级：物理科学逻辑因果倒置判定、长句语义放行、Word选填括号自动提取与防碰撞拆字关键词

#### 1. 科学规律因果不可倒置（因果颠倒一票否决 0 分）
- **根治自变量/因变量颠倒误判**：
  - 科学实验中自变量（条件/原因）在前、因变量（结论/结果）在后。
  - “反射角等于入射角”为正确客观规律；“入射角等于反射角”判定因果倒置，一票否决直接判 0 分。
  - “电流与电压成正比”为正确规律；“电压与电流成正比”判定因果倒置，一票否决直接判 0 分。
- **长句等价语义放行满分**：
  - 废除底层 `max_exp_len <= 8` 暴力截断限制。当学生作答“反射角的度数和入射角的度数相等”时，因果逻辑正确、语义完全等价，大模型判满分予以保留通过。

#### 2. Word题干横线后括号“（”自动识别选填与唯一答案
- **Word 题干智能结构化解析**：
  - 自动识别题干横线（空位）后跟有“（”的特征（如 `______（选填“放大”或“缩小”）`、`______（变大/变小/不变）`、`______（选填...）`）。
  - 自动为每个空位标记为**选填题 (`choice_fill = True`)**并勾选**唯一答案 (`unique_answer = True`)**。

#### 3. 错误选项与“防碰撞”拆字关键词自动生成
- **未选选项自动填入【常见错答案】**：
  - 题干括号内的其余未选备选项，自动提取并写入对应空的【常见错答案】。
- **独创字符差集过滤机制（彻底杜绝误杀）**：
  - 自动将错误选项拆字为【错误关键词】，并**严格过滤掉在正确答案中出现的字符**。
  - 示例：正确答案为“变大”，错误选项为“变小”；过滤共有字“变”，错误关键词仅填入“小”；学生答“变大”绝不会因为命中“变”字被误杀！

#### 4. 答案方案配置新增【唯一答案】与【选填题】配置列
- **双窗口同步新增配置项**：
  - 答案方案主配置表格与 OCR 空位高级设置窗口均增加【唯一答案】与【选填题】复选列，支持勾选、记忆与联动保存。
  - 内置物理学科专有名词库（光学、声学、力学、热学、电磁学），输入或匹配到专业术语自动勾选【唯一答案】。
- **OCR 拆字判错严格绑定【唯一答案】**：
  - OCR 拆字判错仅在勾选【唯一答案】的客观题上生效，开放性与趋势题严禁拆字判错。

#### 5. 选填题与自由填空变化趋势差异化判分
- **选填题**：必须从限定选项中选词，严禁用近义词替代（如选项为“增大”，学生写“变大”判 0 分）。
- **自由填空题**：同向变化趋势词（“增大”与“变大”、“增加”、“升高”）一律判对给满分。

---

## [v2.7.2] - 2026-09-09

### 🛡️ 重点升级：大模型防“和事佬”语义放水、防脑补自动加字与代码级严格卡扣

#### 1. 根治大模型“自动加字”与“语义过宽放水”缺陷
- **严禁把题干印刷体计入学生答案**：绝对严禁将任何卷面印刷体当成学生答案，仅提取手写笔迹。
- **严禁根据标准答案擅自补全（一字不增，如实转写）**：学生漏写关键限定词（如“点上方”缺“S”），严禁擅自补上，必须如实转写并判 0 分。
- **杜绝“和事佬”同情分与近义词放水**：严禁用生活口语替代科学术语，错别字、漏字一律判 0 分。

#### 2. 系统代码层“双重严格兜底校验”（Post-Grading Hard Verification）
- 在极严格模式下，大模型判满分后，Python 代码底层二次严格核对，字面不符或漏字直接一票否决降为 0 分。

#### 3. 设置面板提供【分级严格度】与【自定义防放水 Prompt 指令】
- 极严格模式（默认）、标准学术模式、宽容理解模式；支持自定义限制文本与一键模板。

#### 4. 多源标准答案自动回退兜底
- 自动从已有记录的 `auto_grade_expected` 中回退提取真实标准答案，杜绝大模型盲猜。

---

## [v2.7.1] - 2026-09-09

### 🔍 重点新增：大模型识别效果可视化查看与单题即时检验工具

#### 1. 全透明查看大模型识别文本、去涂改细节与评分理由
- **主观题批改界面深度融合**：
  - 试卷题目由大模型批改后，状态栏显示 `🤖 大模型(qwen-vl-plus)已批`。
  - OCR 建议栏自动切换为高亮专属紫色（`#6B21A8`），完整展示大模型识别出的真实手写字以及判定理由（例如：`【已滤除涂改】学生作答为“反射”，与参考答案一致`）。
  - 让阅卷老师对大模型“认了什么字”、“有没有忽略划掉的错字”、“给分的理由是什么”一目了然、心明眼亮。

#### 2. 全新【🔍 大模型智能识图效果实测预览】独立交互窗口 (`open_llm_preview_dialog`)
- **随时测试、即时体验**：
  - 在【⚙ 大模型设置】、【OCR识别结果分类】中均可点击 **`[🔍 试卷切片效果实测]`**。
  - 在【主观题批改】操作栏中增设 **`[🔍 大模型试判当前题]`** 与 **`[🔍 大模型试识别当前空]`**。
- **全景式图文评判卡片**：
  - 📸 **切片高清原图**：居中清晰展示手写作答局部。
  - 🤖 **识别学生作答**：以 13pt 大号深蓝加粗字体直观呈现实测文字。
  - ✍️ **涂改/划线检测**：自动高亮标明 `⚠️ 检测到涂改/划线痕迹（已自动智能忽略被涂改文字）` 或 `✅ 无涂改痕迹（文字整洁）`。
  - ⚖️ **判定得分**：根据标准答案自动折算得分（带绿/蓝/红色彩标签）。
  - 💡 **详细判定理由**：呈现大模型对学生答案的语义推导与给分依据。
  - ⏱️ **性能监控**：实时呈现接口请求耗时（如 `1.15 秒`）与当前调用模型。
- **单题即时采纳功能**：
  - 老师在人工批改界面遇到疑难或字迹潦草作答时，一键调出大模型试判，满意后点击 **`[✅ 采纳此判定并填入当前题]`**，即可瞬间完成该题自动录分入库！

---

## [v2.7.0] - 2026-09-09

### 🤖 重点新增：接入多模态大模型（通义千问 Qwen-VL-Plus）智能批改与涂改过滤

#### 1. 解决核心痛点：学生作答“涂改/划线重写/橡皮擦除印记”自动识别
- **痛点**：传统本地 OCR 会将划线、涂黑块机械识别为标点或乱码，把划掉的错字与订正答案粘连拼凑，导致大量试卷沦为待人工打分。
- **大模型突破**：接入阿里云百炼专属接口的多模态视觉大模型（`qwen-vl-plus`），通过专业阅卷 Prompt 自动进行视觉去噪：
  - 自动识别并忽略横线、斜杠删除线与黑块涂改；
  - 精确提取学生最终保留或在上方/旁边订正的书写文字；
  - 结合题目题位、满分、标准参考答案与核心采分点，一步到位判定得分（0 到满分），并生成人类教师级别的简明评语。

#### 2. 一键批量智能批改与多线程并发
- **入口位置**：
  - 【主观题待人工打分】窗口：增设 `【🤖 大模型智能批改剩余题】` 与 `[⚙ 大模型设置]`。
  - 【OCR识别结果分类】窗口：增设 `【🤖 一键大模型智能批改所有剩余待人工题】` 与 `[⚙ 大模型设置]`。
- **极速并发与可视化交互**：
  - 采用 Python 原生多线程并发（默认 3~5 并发），全班剩余待人工题通常在 10~20 秒内批量完成。
  - 弹出非模态进度框，实时显示当前题位、学生姓名、识别得分与涂改过滤提示。
  - 批改完成后弹窗汇总报告（成功数、滤除涂改数、失败数），自动更新成绩并即时刷新界面。
  - 支持中途安全点击 `[停止批改]`，已完成部分自动保存入库。

#### 3. 大模型专属配置与连通性测试面板
- 新增 `app/llm_grading_config.json` 与设置对话框（`open_llm_grading_settings`）。
- 默认预置用户专属百炼 API 接口，支持随时切换模型（`qwen-vl-plus`、`qwen-vl-max`、`qwen3-vl-plus` 或本地 Ollama）。
- 内置 `[⚡ 测试连接]` 按钮，毫秒级检测网络连通性并显示模型返回状态。

---

## [v2.6.0] - 2026-09-09

### 🚀 重点新增与重大升级

#### 1. OCR 识别结果分类：彻底分离“单项看图即判”与“就地右键批量判分”
- **单项看图即判（单份试卷独立批阅）**：
  - **界面专属面板**：在右侧样张图片的正右方，设立专属【看图即判 (单份)】操作面板。
  - **大号彩色触控按钮**：
    - 绿色大按键：`1 分\n[当前图片]`（快捷键 `1`）
    - 红色大按键：`0 分\n[当前图片]`（快捷键 `0`）
    - 蓝色大按键：`满分\n[当前图片]`（分值 > 1 分时自动显现）
  - **极速键盘盲打**：老师目光仅需注视图片，手指按主键盘或小键盘数字键 `1` 或 `0`，即可直接判分。
  - **流水线自动连续切图**：
    - 判分后自动保存当前学生成绩，并从该分类的待办中扣减；
    - 左侧列表该项剩余份数即时递减（如 5 份 ➔ 4 份）；
    - 右侧图片**瞬间自动切换至该分类下的下一位学生作答图片**，无需老师手动点击翻页；
    - 当该分类最后一张作答判完后，该分类自动移出列表，系统自动选中并切到下一分类的第一张图片。
  - **解决痛点**：有效避免不同学生虽然 OCR 识别文本相同，但字迹或细节书写不一致时的误判问题。

- **就地右键菜单（整组/批量秒级判分）**：
  - **光标就地弹出**：在左侧列表中任意一行或多选行上右键单击，直接弹出快捷菜单。
  - **明确批量范围提示**：
    - 单选：`⭐ 该类所有试卷直接判 1 分 (共 N 份全部给 1 分)`、`⭐ 该类所有试卷直接判 0 分`
    - 多选：`⭐ 所选 M 类所有试卷批量判 1 分 (共 N 份全部给 1 分)`
  - **秒级一网打尽**：确认一整类完全正确或完全错误时，一键整组 10~50 份试卷瞬间完成批改。

#### 2. OCR 分类列表复选/多选与一键快捷批量归类
- **复选框交互（☑ / ☐）**：
  - 列表首列新增复选框，支持单击单元格勾选、双击整行切换勾选。
  - 支持键盘 **`Space`（空格键）** 极速切换勾选状态。
- **快捷工具栏**：
  - 顶部增加 `[☑ 全选]`、`[☐ 全不选]`、`[🔄 反选]` 快捷按钮，实时汇总勾选分类数与涵盖试卷数。
- **区域多选兼容**：全面兼容 `Ctrl` 逐项多选与 `Shift` 区域连选。
- **一键快捷批量绑定**：
  - 底部面板提供 `[✔ 设为备选答案]`、`[✔ 设为答案关键字]`、`[✖ 设为错误答案]`、`[✖ 设为错误关键字]`。
  - 支持 `[☑ 同时存入长效记忆库]`，一次操作同时更新当前试卷并沉淀为跨试卷通用的永久规则。

#### 3. 软件关闭自动记忆与重启断点续接
- **自动记忆上次测试任务**：
  - 无论是正常关闭窗口、完成批改退出，系统均会自动将当前测试的 `session_id`、文件夹路径、当前选项卡与样张查看进度持久化保存。
- **重启无缝自动恢复**：
  - 再次启动软件时，在 100ms 内静默加载恢复上次关闭前的批改任务（包括 47 份试卷批改结果、分数统计视图、答案方案及主观题样张），无需老师手动去“文件 -> 打开测试”重新选择。
  - 状态栏即时提示：`已自动恢复上次任务：《测试班级\4-6章》（共 47 份试卷），方便您继续工作。`
- **主动清空保护**：
  - 老师若点击菜单“清空当前界面”，系统识别为主动作业结束，下次启动保持干净初始界面。
- **Python 3.14 兼容性**：
  - 修复 Python 3.14 下 `Path('.').with_name(...)` 在空路径时报错的问题。

#### 4. OCR 智能二次批改与拆字容错增强
- **拆字容错判错（`split_char_check`）**：
  - 对学生填空作答进行字符级拆解分析，若作答内容完全未命中标准答案字，系统自动按拆字判错给 0 分。
- **空白填空自动处理**：
  - 未识别到文字的空白填空，自动标记为空白并给 0 分，免去老师逐份人工确认空白的繁琐步骤。
- **一键二次批改**：
  - 支持点击 `[⚡ 用当前全套设置二次批改所有待人工题]`，自动以最新设定的备选答案、关键词与长效记忆库进行全量重新比对赋分。

#### 5. 试卷题库与 Word (Docx) 智能标准答案解析
- 支持从 Word 试卷文档智能提取大题、小题、题型、分值与标准答案。
- 增强括号拆分与候选词智能匹配富集，提升题库答案导入自动化程度。

---

## [v2.5.0] - 2026-08

### 🌟 功能特性
- **主观题长效记忆库机制**：
  - 建立标准答案与错误答案长效记忆库（`ocr_preference_memory.json`），支持多试卷通用共享。
- **多模板支持**：
  - `direct_paper_choice_v1`（试卷直接批改模板，支持双面直批）；
  - `mixed_objective_subjective_v1`（客观主观混合模板）；
  - `mixed_objective_subjective_v2_edge`（边缘定位块模板）；
  - `错题采集模板_tc`（错题本采集）。

---

## [v2.0.0] - 2026-07

### 🌟 核心基础
- **多引擎 OCR 识别支持**：
  - 集成离线 PaddleOCR 与本地识别能力，自动裁剪主观题横线区域进行文本识别与自动给分。
- **统计报表与错题本生成**：
  - 班级分数统计、各题得分率分析、错题本 PDF 自动排版导出。
- **SQLite 数据库持久化**：
  - 本地单会话 / 多测试持久化存储，支持随时导出与还原。
