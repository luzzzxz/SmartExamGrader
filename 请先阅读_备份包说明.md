# 完整备份包说明

> 文档版本：2026-09-08

这个压缩包用于恢复答题批改系统，属于私有备份，不应公开分享。

包内结构：

```text
app/                         主程序、当前模板、答案方案和批改进度
runtime/pydeps314/           Python 3.14 常规依赖
answer_card_stable_bootstrap.py
answer_card_cv_helper.py
启动答题批改系统.bat
安装OCR环境.bat
另一台电脑安装说明.md
软件更新日志.md              版本演进、功能新增与修复记录
PACKAGE_MANIFEST_SHA256.txt  文件清单和校验值
```

启动：

1. 安装 64 位 Python 3.14。
2. 将压缩包完整解压到任意可写目录。
3. 双击 `启动答题批改系统.bat`。

常规依赖已经放在 `runtime/pydeps314`。PaddleOCR 虚拟环境和模型没有放入压缩包，因为二者约 1.2GB，而且虚拟环境可能记录旧电脑的 Python 路径。

需要 OCR 时，另行安装 64 位 Python 3.13，然后双击包根目录的 `安装OCR环境.bat`。环境建立在：

```text
app/paddleocr_env
```

完整步骤和故障排查见包根目录的 `另一台电脑安装说明.md`。版本见 `app/requirements-paddleocr.txt`，模型可由 PaddleOCR 下载到当前用户目录的 `.paddlex/official_models`。

此包包含：

- 当前测试数据库。
- 班级与学生信息。
- 答案方案和方案模板图。
- 测试绑定的答案、结构和主观题进度。
- 网页同步令牌与服务配置。
- 当前知识点编号总表的便携副本。
- 班级名册提取工具和 VPS 网页服务部署文件。
- 最新主程序使用帮助与维护说明。

即使打包时主程序仍在运行，打包脚本也会通过 SQLite 在线备份接口生成一致的数据库快照，避免直接复制正在写入的数据库造成进度缺失。

因此只能作为个人备份保存。上传网盘时应使用可信位置并设置访问权限。
